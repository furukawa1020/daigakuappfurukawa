name: daigaku_os_client
description: "DaigakuOS v2 Client (Zero Input)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0
  http: ^1.2.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  # For Geofencing later (commented out until needed)
  # background_fetch: ^1.3.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
